{
  "name": "rocket-bank",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w @rocket/api",
    "build:api": "npm run build -w @rocket/api",
    "start:api": "npm run start -w @rocket/api",
    "dev:web": "npm run dev -w @rocket/web",
    "build:web": "npm run build -w @rocket/web",
    "start:web": "npm run start -w @rocket/web",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down -v",
    "prisma:generate": "npm run prisma:generate -w @rocket/api",
    "prisma:migrate": "npm run prisma:migrate -w @rocket/api"
  }
}

